# 项目文档约定

- `docs/` 仅用于在 GitHub 公开的项目文档：接入指南、API、架构、可复用的构建与发布方法。
- 过程文档、执行计划、发布准备记录、实验结果、质量回测、设计审查稿、审查提示词和会话进度统一放在 `.docs-private/`。该目录由 Git 忽略，禁止强制加入版本控制。
- README、文档索引和其他公开文档不得链接到 `.docs-private/` 中的文件。
- 工具目录保留代码、测试数据清单和可复用的使用说明；本地实验结果和阶段记录放在 `.docs-private/`。正式版本公开基准仍按 `benchmark/data/` 的归档约定维护。

# 发行体积优化门槛

- 以最终 AAR 压缩后的实际文件大小衡量收益。缩减不足 1% 的实验视为没有值得保留的优化，不为此修改正式代码或构建配置。

# 版本基准覆盖

- 采集与展示分离：每个版本按固定协议只采集当前版本，归档后作为不可覆盖的原始数据。README、版本对比、发布说明统一从归档生成；调整展示不触发真机测试。旧版本直接使用已归档基线，不自动配对重跑。缺项、协议/模型/设备差异应明确标注；只有用户明确要求补测时才新增采集。

- 保留历史基准及其协议定义。后续常规测量按用户要求不再复测四线程，使用 `android-native-v2` 的七场景和 `android-app-v2` 的六场景（ML Kit、单线程、双线程），仍保留既定的 200 条输入、三轮三遍及全部指标。新特性测试可以追加，不能缩减其余基准覆盖。
- README 性能主表只展示单线程、双线程和 ML Kit；四、六线程仅提供明确标注、说明假设的估算，通常推荐双线程。线程增加的收益受并行比例、内存带宽和调度影响，不按线程数线性外推。
- README 的版本性能数据应来自相同协议的完整测量。保留失败、缺项和稳定性限制；不得将不同协议的结果直接换算成版本性能提升。
