# vX.Y.Z 性能记录

> 复制到版本目录，填写本次记录。以下各行和各列均须保留；没有证据时写“未测”，不要填估计值或 0。每台设备单独使用同一张表。旧报告可保留原文，补充标准表时也必须注明来源和缺项。

## 版本与测量条件

- 版本 / commit / native source SHA-256：未填
- 状态：未测 / 部分完成 / 完整通过 / 失败
- 测量日期、原始 JSON 路径、被比较版本：未填
- 清单：`android-native-v1`；清单摘要取自报告 `suite_sha256`
- 设备型号 / SoC / 系统 / 设备指纹：未填
- NDK / CMake / 构建类型 / ABI / harness SHA-256：未填
- 模型、词表、shortlist、前缀表、语料 SHA-256：见原始 JSON，不只填写版本名
- 固定输入：FLORES-200 前 200 行；每版本 3 个独立进程，每进程 3 遍；AB / BA / AB
- CPU 亲和性、起止温度/频率、降频和异常：未填

## 固定结果表（每台设备一份）

单位、计时范围与统计方法遵循测试清单。指标均为三进程中位数，热态先对每进程后两遍取中位数。峰值是首遍后的 native VmHWM，不是 app PSS。

| 场景 ID | 首次耗时 ms | 热态耗时 ms | 峰值 RSS MiB | 输出哈希 / 稳定性 | 状态 |
|---|---:|---:|---:|---|---|
| `enzh_w1` | 未测 | 未测 | 未测 | 未测 | 未测 |
| `enzh_w2` | 未测 | 未测 | 未测 | 未测 | 未测 |
| `enzh_w4` | 未测 | 未测 | 未测 | 未测 | 未测 |
| `pivot_w1` | 未测 | 未测 | 未测 | 未测 | 未测 |
| `enzh_b512p` | 未测 | 未测 | 未测 | 未测 | 未测 |
| `pivot_b512p` | 未测 | 未测 | 未测 | 未测 | 未测 |
| `enzh_w2_512p` | 未测 | 未测 | 未测 | 未测 | 未测 |
| `pivot_w2_512p` | 未测 | 未测 | 未测 | 未测 | 未测 |

## 同配置版本比较

- 对外优先展示首次/热态翻译速度（条源文/秒）及速度倍率、提升百分比。速度 = `输入条数 × 1000 / 上表耗时 ms`；倍率 = `旧版耗时 / 新版耗时`，提升 = `(倍率 − 1) × 100%`。上表原始耗时与全部场景保留，不改清单定义。
- 使用相同清单、设备与输入的两个完整报告；按相同场景 ID、相同指标逐项比较。
- 记录检查命令、阈值、退出码以及每项差值，不只摘录提升最大的场景。
- 缺项标“不可比较”，保留该行；不把不同配置或不同设备数字混在同一差值中。
- 未补齐证据时，这里不能写“通过”。

## 默认参数收益（可选，单独报告）

若各版产品默认值改变，明确列出旧/新参数及测试来源。这部分不是上面的同配置结果，也不代替固定八场景。

## 新增维度与限制

新增维度只追加；保留既有定义与旧清单，并升级清单 ID。旧版尚无新维度数据时标“未测”，补测后新增带日期的记录，不覆盖原始结果。

记录失败、未支持项、输出差异及未重算的质量指标。
